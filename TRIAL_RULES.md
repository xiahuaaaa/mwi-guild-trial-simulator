# 公会试炼规则基线

更新时间：2026-08-28  
来源：用户提供的游戏内公会试炼说明；生活行动公式对齐迷宫生活/强化房间；
进度需求由 2026-07-28 实况截图校准；等级 UI 语义由 2026-08-04 战斗试炼实况修正。

## 通用规则

- 公会周每周五 `00:00 UTC` 重置；每周随机获得 4 场生活试炼和 2 场战斗试炼。
- 每位成员每周最多报名 1 场生活试炼和 1 场战斗试炼。
- 只有在本公会周开始前加入公会的成员才有参与资格。
- 每场试炼最多运行 1 小时活跃时间，且不会打断角色的正常行动。
- 第一层为 `Lv.100`，每通关一层增加 10 级，最高 `Lv.300`。

### 公会界面等级 vs 实际房间（生活 / 战斗相同）

- 试炼卡片上的 `Lv.X` = **已经完成的最高层**（通关进度），不是正在打的房间。
- **实际进行中的房间** = `X + 10`（下一层）。
- 进度分母 / Boss HP·MP / 成功率里的 `level` / `roomLevel` **一律用实际房间等级**，不要直接用卡片 `Lv.X`。
- 例（2026-08-04 战斗）：卡片「试炼獾 Lv.160，1696 点」时，怪物面板与血条为 **Lv.170**，
  `maxHP = 820800`、`maxMP = 540000`（52 人）。
- 作废此前误读：把卡片等级当成公式等级、或写成「公式 level = UI − 10」。
  正确关系是 `currentRoomLevel = guildCardCompletedLevel + 10`（尚无通关时卡片可显示
  首层 `100`，此时房间也是 `100`）。

## 生活试炼

机制对齐迷宫生活房间（含强化房间特例）。使用成员选择配装的快照；
食物/饮料等消耗品不可用。茶叶箱（补给箱）类增益按迷宫同名字段计入。

### 模拟与分工门槛（公会内部约定）

生活分工推荐与生活模拟：成员**对应专业**等级需达到模拟门槛才纳入计算
（挤奶 **≥ 85**；其他非制作专业 **≥ 90**；**制作 ≥ 100**）。此为模拟器筛选规则，非游戏内硬性报名限制。

### 共享进度（非强化）

- 所有报名成员的专业行动进度进入共享池。
- 必须填满进度条才能通过该层。
- 每位报名参与者使该层所需总工作量增加 `1%`：

  ```text
  unscaledProgress = 400 × (level + 20)
  scaledProgress = floor(unscaledProgress × (1 + participantCount × 0.01))
  ```

  公式中的 `level` = **实际房间等级**（见上方「公会界面等级 vs 实际房间」），
  不是卡片上的已完成等级。
  已用截图校准：实际房间 Lv.120 / 22 人 → `68320`；Lv.130 / 22 人 → `73200`。
  其他层仍建议再截图复核。

### 行动时间

- 非强化生活试炼基础行动时间 `10` 秒（与迷宫非强化生活房间一致）。
- 强化生活试炼基础行动时间 `8` 秒：公会试炼把强化改成与其他房间相同的
  共享进度条（“血量”）机制后的补偿。
- 实际工作时间：`baseActionSeconds / (1 + 行动速度)`。
- 截图参考：挤奶约 `4.6s`，制作约 `4.54s`（按 10s 基时反推速度）。

### 成功率

```text
levelDiff = effectiveLevel - roomLevel
levelBonus = levelDiff >= 0 ? levelDiff × 0.005 : levelDiff × 0.01
successRate = clamp01(0.8 × (1 + levelBonus + successBonus))
```

- 基础 `80%`。
- 每低于房间 1 级：`levelBonus −1%`（即乘到括号内）。
- 每高于房间 1 级：`levelBonus +0.5%`。
- 专业等级加成、茶叶箱增益和其他加成可提高 `effectiveLevel` 或直接提供
  `successBonus`。

### 双倍进度

成功时有概率使本次进度翻倍：

```text
doubleProgressChance = clamp01(补给箱增益 + 采集加成 + 美食加成)
```

- 采集加成：挤奶、伐木、采摘（foraging）；装备 `gatheringQuantity` 计入。
- 美食加成：烹饪、酿造（brewing）；来源是 `/buff_types/gourmet`，
  **不是** rare-find。成员快照暂无房屋/社区美食 buff → 模拟暂按 `0`。
- 截图参考：挤奶双倍进度 `31.34%`（含采集/补给；非装备 rare-find）。

### 进度 / 工作力

```text
workForce = floor(effectiveLevel × (1 + efficiency))
```

- UI「工作能力」即每次成功写入共享池的进度。
- 成功且触发双倍时写入 `2 × workForce`。
- 截图参考：挤奶 `134`，制作 `152`。

### 强化房间

- 公会试炼强化与其他生活房间相同：共享进度条、
  `scaledProgress = floor(400×(level+20)×(1+人数×0.01))`、成功率 / 工作力 / 双倍公式相同。
- 唯一差异：基础行动时间 `8` 秒（其他生活专业 `10` 秒）。
- 迷宫插件仍可能按旧「强化等级 +1/−1/+2」模拟迷宫房间；那是迷宫玩法，
  **不要**套回公会试炼。

## 公式交叉参考

生活行动公式（成功率 / 工作力 / 双倍；非强化基时 10s）对照：

- Greasy Fork：https://greasyfork.org/zh-CN/scripts/566829 （迷宫胜率计算器 v1.5.10）
- 本地缓存（不入库）：`guild-trial-simulator/.local/ref/labyrinth-winrate-566829.user.js`
- 注意：迷宫强化房间机制与公会试炼强化进度条不同；公会侧以本文件为准。

## 战斗试炼

- 遭遇类型为獾、变色龙、水母、刺猬或虫群之一。
- 默认每层一只怪物；**试炼獾**已确认每层两只同级獾，两只都死后才换层。
- 每位报名参与者使**每只**怪物更强（2026-08-14 `v1.20260813.0`，面板原文；完整事件引擎已按此缩放）：

  ```text
  scaledHp = floor(unscaledHp × (1 + n × 0.01))
  attackInterval = baseInterval / (1 + n × 0.02)
  castSpeed = baseCastSpeed + n × 0.02
  abilityHaste = baseHaste + n × 2
  ```

  `n` = 报名人数。攻速 +2% 是独立的 attack-speed 除数（与攻击等级提供的那档分开）。
  技能急速 +2 是扁平点数，不是百分比。基础急速已从 80 降到 0，基础攻速/施法也大幅变慢。
- 高层 HP/MP 池相对 Lv.100 面板按已确认公式成长（不是迷宫 `level/100`）：
  `unscaledPool = floor(level100Pool × (level + 10) / 110)`。
  此处 `level` = **实际房间等级**（卡片已完成等级 + 10）。
  MP 池同样按该式成长，但**不**乘人数；仅 HP 再乘 `(1 + n × 0.01)`。
  例如水母 Lv.100 = 495000，15 层（Lv.240）未乘人数 = 1125000；
  47 人时 HP = 1653750，MP 仍为 1125000。
  2026-08-04 獾：Lv.100 池 330000；卡片已完成 160 → 房间 170 →
  `unscaled = 540000`，52 人 `scaledHp = 820800`（UI 缩写 820K），MP `540000`。
- 食物和饮料不可使用，改为每 10 秒 HP/MP 回复率在原值上各增加 `0.03`。
  这不是将装备或技能提供的回复率整体乘 4；只有无额外回复时，
  基础 `1% + 3%` 才表现为原基础回复的 4 倍。
- 每次受到攻击最多进行 5 次格挡（上游源码名为 `parry`）判定。
- 击杀当前层全部怪物后进入下一层；任一层失败、本场累计 1 小时耗尽或通关
  `Lv.300` 时结束。
- 击杀当前层、进入下一层时，所有参与者的 HP 和 MP 自动补满。
  当前只确认 HP/MP；技能冷却、Buff、Debuff、护盾和施法状态仍需校准。

## 点数和代币

- 生活试炼基础点数：首层 200，之后每层 100。
  `lifeBase = levelsCleared === 0 ? 0 : 200 + (levelsCleared - 1) * 100`
- 战斗试炼基础点数：首层 400，之后每层 200。
  `combatBase = levelsCleared === 0 ? 0 : 400 + (levelsCleared - 1) * 200`
- 公会点数：`基础点数 × (1 + 建造者殿堂加成)`。
  - **战斗**：2026-08-04 结束态精确吻合（獾 `3200×1.06=3392`，刺猬 `2600×1.06=2756`）。
  - **生活**：层数步进与 `+100×(1+加成)` 增值结构正确，但卡片显示点相对
    `lifeBase×1.06` 系统性偏低约 14～16（例：Lv.190 期望 1166、实况 1152）。
    取整/隐藏修正未解；代币仍应按官方「基础点数」口径，勿把偏差卡片点当 base。
- 每名符合资格成员的代币：
  `0.5 × 全部试炼基础点数总和 × (1 + 金库加成)`。
- 参与奖励代币为上述符合资格成员代币的 `50%`。

### 报名人数上限（会成长，须同步）

- 2026-07-28 实况：生活试炼 **24** 人/场，战斗试炼 **48** 人/场（总览分母）。
- 上限随公会成长变化；权威来源为插件从游戏同步的 `maxParticipants`，禁止长期写死。

## 仍需校准

- 生活试炼 `400 × (level + 20)` 进度公式在 Lv.100 / 其他层的更多截图复核。
- 生活试炼强化专业的目标强化等级表（迷宫按房间等级映射，试炼是否同表）。
- 生活试炼茶叶箱/补给箱在正式服是否可用，以及与配装快照的叠加顺序。
- 等级成长的攻击/防御/技能等级等非 HP/MP 面板是否完全等同迷宫 `roomLevel/100` 公式。
- 换层时技能冷却、Buff、Debuff、护盾和施法状态。
- 失败、死亡和复活的精确状态机。
- 多名格挡者的候选排序；当前模拟按存活格挡者随机无放回选择，
  最多尝试 5 名。
- 公会建筑、神龛和光环的精确叠加顺序。

已确认：Boss HP/MP 池为 `floor(level100Pool × (level + 10) / 110)`，再对 HP 施加人数 +1%。
已确认（生活）：成功率/双倍进度/工作力对齐迷宫；进度需求由 Lv.120/130 截图校准。
